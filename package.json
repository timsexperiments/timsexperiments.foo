{
  "name": "timsexperiments",
  "type": "module",
  "devDependencies": {
    "bun-types": "1.4.2",
    "turbo": "2.10.13"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "private": true,
  "packageManager": "bun@1.4.2",
  "engines": {
    "bun": "1.4.2",
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "turbo run dev --filter=apps-timsexperiments",
    "build": "turbo run build",
    "build:site": "turbo run build --filter=apps-timsexperiments",
    "check": "turbo run check",
    "test": "turbo run test"
  }
}
